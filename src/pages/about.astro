---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Sobre Nós - Pecaditos">
	<!-- About Hero -->
	<section class="about-hero">
		<div class="container">
			<div class="hero-content">
				<h1 class="hero-title">A Nossa História</h1>
				<p class="hero-subtitle">Conhece a paixão por trás dos Pecaditos</p>
			</div>
		</div>
	</section>

	<!-- Our Story -->
	<section class="our-story">
		<div class="container">
			<div class="story-content">
				<div class="story-text">
					<h2>Como Tudo Começou</h2>
					<p>
						A <strong>Barraquinha dos Pecaditos</strong> nasceu do sonho de um jovem casal apaixonado por criar momentos doces e especiais. Começámos esta aventura com uma simples missão: provocar sorrisos através de doces únicos e irresistíveis.
					</p>
					<p>
						Cada receita é desenvolvida com carinho, testada com amor e aperfeiçoada até atingir a perfeição. Acreditamos que os melhores doces são aqueles feitos com ingredientes de qualidade e uma pitada generosa de paixão.
					</p>
					<p>
						O que começou como uma pequena ideia entre dois corações apaixonados, transformou-se numa marca que espalha alegria pelas <strong>Terras de Basto</strong> e muito além.
					</p>
				</div>
				<div class="story-visual">
					<div class="couple-illustration">
						<div class="heart-float">💕</div>
						<div class="couple-emoji">👫</div>
						<div class="sparkles">✨</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Our Mission -->
	<section class="our-mission">
		<div class="container">
			<div class="mission-content">
				<div class="mission-visual">
					<div class="mission-icon">🎯</div>
					<div class="floating-elements">
						<span class="float-element">🍰</span>
						<span class="float-element">❤️</span>
						<span class="float-element">😊</span>
					</div>
				</div>
				<div class="mission-text">
					<h2>A Nossa Missão</h2>
					<p>
						A nossa missão é simples mas poderosa: <strong>criar momentos de pura felicidade através dos nossos doces</strong>. Queremos que cada mordida seja uma experiência única, que desperte sorrisos e crie memórias saborosas.
					</p>
					<p>
						Trabalhamos incansavelmente para oferecer produtos de qualidade superior, sempre com um toque de inovação e criatividade que nos distingue. Cada doce é uma pequena obra de arte comestível.
					</p>
					<div class="mission-highlights">
						<div class="highlight">
							<span class="highlight-icon">🌟</span>
							<span>Qualidade Premium</span>
						</div>
						<div class="highlight">
							<span class="highlight-icon">💝</span>
							<span>Feito com Amor</span>
						</div>
						<div class="highlight">
							<span class="highlight-icon">🎨</span>
							<span>Criatividade Única</span>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Our Values -->
	<section class="our-values">
		<div class="container">
			<h2 class="section-title">Os Nossos Valores</h2>
			<div class="values-grid">
				<div class="value-card">
					<div class="value-icon">❤️</div>
					<h3>Paixão</h3>
					<p>Cada doce é criado com amor e dedicação. A nossa paixão pela doçaria é o ingrediente secreto que torna tudo especial.</p>
				</div>
				<div class="value-card">
					<div class="value-icon">🌟</div>
					<h3>Qualidade</h3>
					<p>Utilizamos apenas os melhores ingredientes e técnicas artesanais para garantir a máxima qualidade em cada criação.</p>
				</div>
				<div class="value-card">
					<div class="value-icon">🎨</div>
					<h3>Criatividade</h3>
					<p>Inovamos constantemente, criando sabores únicos e apresentações que surpreendem e encantam os nossos clientes.</p>
				</div>
				<div class="value-card">
					<div class="value-icon">🤝</div>
					<h3>Proximidade</h3>
					<p>Valorizamos a relação pessoal com cada cliente, criando uma experiência calorosa e acolhedora em cada interação.</p>
				</div>
				<div class="value-card">
					<div class="value-icon">🌱</div>
					<h3>Sustentabilidade</h3>
					<p>Comprometemo-nos com práticas sustentáveis, apoiando produtores locais e minimizando o nosso impacto ambiental.</p>
				</div>
				<div class="value-card">
					<div class="value-icon">😊</div>
					<h3>Alegria</h3>
					<p>O nosso objetivo é espalhar felicidade através dos nossos doces, criando momentos de pura alegria e satisfação.</p>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* Add global box-sizing and overflow prevention */
	* {
		box-sizing: border-box;
	}

	html, body {
		overflow-x: hidden;
		max-width: 100vw;
	}

	/* About Hero Section */
	.about-hero {
		background: linear-gradient(135deg, var(--cream) 0%, var(--beige-light) 50%, var(--beige-dark) 100%);
		padding: 4rem 0;
		text-align: center;
		position: relative;
		overflow: hidden;
		width: 100%;
	}

	.about-hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="15" cy="15" r="1" fill="rgba(60,36,21,0.1)"/><circle cx="85" cy="35" r="1.5" fill="rgba(60,36,21,0.1)"/><circle cx="35" cy="85" r="1" fill="rgba(60,36,21,0.1)"/></svg>');
		animation: float 30s ease-in-out infinite;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
		width: 100%;
		box-sizing: border-box;
	}

	.hero-content {
		position: relative;
		z-index: 1;
	}

	.hero-title {
		font-family: 'Dancing Script', cursive;
		font-size: 4rem;
		color: var(--chocolate-dark);
		margin-bottom: 1rem;
		text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
		word-wrap: break-word;
		overflow-wrap: break-word;
		hyphens: auto;
	}

	.hero-subtitle {
		font-size: 1.3rem;
		color: var(--chocolate-medium);
		opacity: 0.9;
		max-width: 600px;
		margin: 0 auto;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	/* Our Story Section */
	.our-story {
		padding: 5rem 0;
		background: var(--warm-white);
		width: 100%;
		overflow: hidden;
	}

	.story-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		width: 100%;
	}

	.story-text h2 {
		font-family: 'Dancing Script', cursive;
		font-size: 3rem;
		color: var(--chocolate-dark);
		margin-bottom: 2rem;
		position: relative;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.story-text h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 0;
		width: 80px;
		height: 4px;
		background: var(--gold);
		border-radius: 2px;
	}

	.story-text p {
		color: var(--text-light);
		line-height: 1.8;
		margin-bottom: 1.5rem;
		font-size: 1.05rem;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.story-text strong {
		color: var(--chocolate-dark);
		font-weight: 600;
	}

	.story-visual {
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		height: 400px;
		width: 100%;
	}

	.couple-illustration {
		position: relative;
		width: 300px;
		height: 300px;
		display: flex;
		align-items: center;
		justify-content: center;
		max-width: 100%;
	}

	.heart-float {
		position: absolute;
		top: 10%;
		left: 10%;
		font-size: 3rem;
		color: var(--gold);
		opacity: 0.7;
		animation: float 4s ease-in-out infinite;
		animation-delay: 0s;
	}

	.couple-emoji {
		font-size: 5rem;
		position: relative;
		z-index: 2;
		animation: bounce 3s ease-in-out infinite;
	}

	.sparkles {
		position: absolute;
		bottom: 15%;
		right: 15%;
		font-size: 3rem;
		color: var(--chocolate-light);
		opacity: 0.8;
		animation: pulse 3s ease-in-out infinite;
		animation-delay: 1s;
	}

	/* Our Mission Section */
	.our-mission {
		padding: 5rem 0;
		background: linear-gradient(135deg, var(--beige-light) 0%, var(--cream) 100%);
		width: 100%;
		overflow: hidden;
	}

	.mission-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		width: 100%;
	}

	.mission-visual {
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		height: 400px;
		width: 100%;
	}

	.mission-icon {
		font-size: 6rem;
		color: var(--chocolate-dark);
		position: relative;
		z-index: 2;
		animation: pulse 4s ease-in-out infinite;
	}

	.floating-elements {
		position: absolute;
		width: 100%;
		height: 100%;
	}

	.float-element {
		position: absolute;
		font-size: 2rem;
		animation: float 3s ease-in-out infinite;
	}

	.float-element:nth-child(1) {
		top: 20%;
		left: 20%;
		animation-delay: 0s;
	}

	.float-element:nth-child(2) {
		top: 60%;
		right: 15%;
		animation-delay: 1s;
	}

	.float-element:nth-child(3) {
		bottom: 25%;
		left: 15%;
		animation-delay: 2s;
	}

	.mission-text h2 {
		font-family: 'Dancing Script', cursive;
		font-size: 3rem;
		color: var(--chocolate-dark);
		margin-bottom: 2rem;
		position: relative;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.mission-text h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 0;
		width: 80px;
		height: 4px;
		background: var(--gold);
		border-radius: 2px;
	}

	.mission-text p {
		color: var(--text-light);
		line-height: 1.8;
		margin-bottom: 1.5rem;
		font-size: 1.05rem;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.mission-text strong {
		color: var(--chocolate-dark);
		font-weight: 600;
	}

	.mission-highlights {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		margin-top: 2rem;
		width: 100%;
	}

	.highlight {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1rem;
		background: linear-gradient(135deg, var(--cream), var(--beige-light));
		border-radius: 15px;
		transition: all 0.3s ease;
		width: 100%;
		box-sizing: border-box;
	}

	.highlight:hover {
		background: linear-gradient(135deg, var(--beige-light), var(--cream));
		/* Removed translateX to prevent overflow */
	}

	.highlight-icon {
		font-size: 1.5rem;
		flex-shrink: 0;
	}

	.highlight span:last-child {
		font-weight: 600;
		color: var(--chocolate-dark);
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	/* Our Values Section */
	.our-values {
		padding: 5rem 0;
		background: var(--warm-white);
		width: 100%;
		overflow: hidden;
	}

	.section-title {
		font-family: 'Dancing Script', cursive;
		font-size: 3.5rem;
		color: var(--chocolate-dark);
		text-align: center;
		margin-bottom: 3rem;
		position: relative;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.section-title::after {
		content: '';
		position: absolute;
		bottom: -15px;
		left: 50%;
		transform: translateX(-50%);
		width: 100px;
		height: 4px;
		background: var(--gold);
		border-radius: 2px;
	}

	.values-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 2.5rem;
		width: 100%;
	}

	.value-card {
		background: linear-gradient(135deg, var(--cream), var(--beige-light));
		padding: 3rem 2rem;
		border-radius: 20px;
		text-align: center;
		box-shadow: 0 10px 30px rgba(60, 36, 21, 0.1);
		transition: all 0.3s ease;
		border: 2px solid transparent;
		position: relative;
		overflow: hidden;
		width: 100%;
		box-sizing: border-box;
	}

	.value-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, var(--chocolate-light), var(--gold));
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.value-card:hover {
		box-shadow: 0 20px 40px rgba(60, 36, 21, 0.15);
		border-color: var(--chocolate-light);
		/* Removed translateY to prevent overflow */
	}

	.value-card:hover::before {
		transform: scaleX(1);
	}

	.value-icon {
		font-size: 4rem;
		margin-bottom: 1.5rem;
		display: block;
		animation: bounce 2s ease-in-out infinite;
	}

	.value-card h3 {
		font-family: 'Dancing Script', cursive;
		font-size: 2.2rem;
		color: var(--chocolate-dark);
		margin-bottom: 1rem;
		line-height: 1.2;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.value-card p {
		color: var(--text-light);
		line-height: 1.7;
		font-size: 1rem;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	/* Animations */
	@keyframes float {
		0%, 100% { transform: translateY(0px) rotate(0deg); }
		50% { transform: translateY(-15px) rotate(5deg); }
	}

	@keyframes bounce {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-10px); }
	}

	@keyframes pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.1); }
	}

	/* Mobile-First Responsive Design */
	@media (max-width: 768px) {
		.about-hero {
			padding: 3rem 0;
			overflow-x: hidden;
		}

		.hero-title {
			font-size: 3rem;
			word-wrap: break-word;
			overflow-wrap: break-word;
			hyphens: auto;
		}

		.hero-subtitle {
			font-size: 1.1rem;
			word-wrap: break-word;
			overflow-wrap: break-word;
		}

		.container {
			padding: 0 15px;
			max-width: 100%;
			width: 100%;
		}

		.our-story, .our-mission, .our-values {
			padding: 3rem 0;
			overflow-x: hidden;
		}

		.story-content, .mission-content {
			grid-template-columns: 1fr;
			gap: 2.5rem;
			text-align: center;
			width: 100%;
		}

		.story-text h2, .mission-text h2 {
			font-size: 2.5rem;
			word-wrap: break-word;
			overflow-wrap: break-word;
		}

		.story-text h2::after, .mission-text h2::after {
			left: 50%;
			transform: translateX(-50%);
		}

		.section-title {
			font-size: 2.8rem;
			word-wrap: break-word;
			overflow-wrap: break-word;
		}

		.values-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
			width: 100%;
		}

		.value-card {
			padding: 2.5rem 1.5rem;
			margin: 0;
		}

		.value-icon {
			font-size: 4rem;
		}

		.value-card h3 {
			font-size: 2rem;
			word-wrap: break-word;
			overflow-wrap: break-word;
		}

		.story-visual, .mission-visual {
			height: 300px;
		}

		.couple-emoji {
			font-size: 4rem;
		}

		.mission-icon {
			font-size: 5rem;
		}

		.float-element {
			font-size: 1.8rem;
		}

		.heart-float, .sparkles {
			font-size: 2.5rem;
		}

		.highlight {
			padding: 0.8rem;
		}
	}

	@media (max-width: 480px) {
		.container {
			padding: 0 10px;
			width: 100%;
		}

		.hero-title {
			font-size: 2.5rem;
		}

		.hero-subtitle {
			font-size: 1rem;
		}

		.story-text h2, .mission-text h2 {
			font-size: 2.2rem;
		}

		.section-title {
			font-size: 2.5rem;
		}

		.value-card {
			padding: 2rem 1.2rem;
		}

		.value-icon {
			font-size: 3.5rem;
		}

		.value-card h3 {
			font-size: 1.8rem;
		}

		.story-text p, .mission-text p {
			font-size: 1rem;
		}

		.couple-emoji {
			font-size: 3.5rem;
		}

		.mission-icon {
			font-size: 4.5rem;
		}

		.float-element {
			font-size: 1.5rem;
		}

		.heart-float, .sparkles {
			font-size: 2rem;
		}

		.our-story, .our-mission, .our-values {
			padding: 2.5rem 0;
		}
	}

	/* Extra small screens */
	@media (max-width: 360px) {
		.container {
			padding: 0 5px;
			width: 100%;
		}

		.hero-title {
			font-size: 2.2rem;
		}

		.story-text h2, .mission-text h2 {
			font-size: 2rem;
		}

		.section-title {
			font-size: 2.2rem;
		}

		.value-card {
			padding: 1.8rem 1rem;
		}

		.value-card h3 {
			font-size: 1.6rem;
		}

		.couple-emoji {
			font-size: 3rem;
		}

		.mission-icon {
			font-size: 4rem;
		}
	}
</style>