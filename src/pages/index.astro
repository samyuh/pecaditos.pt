---
import Layout from '../layouts/Layout.astro';

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout title="Pecaditos - Doces Tentações das Terras de Basto">
	<!-- Hero Section -->
	<section class="hero">
		<div class="hero-content">
			<div class="hero-text">
				<span class="hero-title">Pecaditos</span>
				<span class="hero-subtitle">O lado Doce do Prazer</span>
				<p class="hero-description">
					Bem-vindos ao mundo dos <strong>Pecaditos</strong>! Somos um jovem casal apaixonado pela criação de algo novo e inusitado, como os doces que trazemos na nossa barraquinha, com o único objetivo de despertar sorrisos, momentos divertidos e atrevidos. Cada criação é feita com amor, safadeza e dedicação para criar um bom momento a quem passe por nós.
				</p>
				<div class="hero-buttons">
					<a href="/menu" class="btn btn-primary">
						<span class="btn-icon">🍫</span>
						Ver Menu
					</a>
					<a href="/contact" class="btn btn-secondary">
						<span class="btn-icon">📞</span>
						Contactar
					</a>
				</div>
			</div>
			<div class="hero-image">
                <img src="/icons/bombom.png" alt="Bombom" class="floating-emoji bombom-img" />
            </div>
		</div>
	</section>

	<!-- Featured Products -->
	<section class="featured-products">
		<div class="container">
			<h2 class="section-title">Os Nossos Pecaditos</h2>
			<div class="products-grid">
				<div class="product-card featured">
					<img src="/icons/crepe.png" alt="Crepe" class="product-icon">
					<h3>Crepiroca</h3>
					<p>Crepes feitos com carinho e um toque especial de irreverência! Disponíveis nas versões tradicionais e "alcoólicas" – com vinho do Porto na massa – e na versão redvelvet. Crepes afrodisíacos, quentes e absolutamente irresistíveis. Podem ser lambuzados simples ou com os diversos toppings disponíveis.</p>
				</div>
				<div class="product-card">
					<img src="/icons/bolas-de-chocolate.png" alt="Chocopau" class="product-icon">
					<h3>Chocopau</h3>
					<p>Fruta em espetadas com chocolate fondue a escorrer…Uma combinação que aquece qualquer ambiente. Ideal para momentos mais doces (e atrevidos!).</p>
				</div>
				<div class="product-card">
					<img src="/icons/bolo.png" alt="Delícia da Madame" class="product-icon">
					<h3>Delícia da Madame</h3>
					<p>Bolos e doces caseiros preparados com amor e muita safadeza, pela Madame. Daquelas sobremesas que só uma trinca já cria saudade.</p>
				</div>
			</div>
		</div>
	</section>

	<!-- About Preview -->
	<section class="about-preview">
		<div class="container">
			<div class="about-content">
				<div class="about-text">
					<h2>Quem Somos</h2>
					<p>
						Somos um casal jovem, guloso e destemido, e juntos criámos os "Pecaditos" – um espaço onde a paixão se serve quente e a satisfação é o ingrediente principal. Decidimos pôr as mãos na massa (literalmente!) e espalhar doçura e atrevimento pelas feiras e festas das Terras de Basto.
					</p>
					<p>
						Mais do que doces, queremos provocar bons momentos, gargalhadas e, claro desejos. Cada produto é pensado para ser um convite ao prazer, um pequeno pecado que vale a pena cometer.
					</p>
					<a href="/about" class="btn btn-outline">
						<span class="btn-icon">❤️</span>
						Conhece a Nossa História
					</a>
				</div>
				<div class="about-visual">
					<div class="heart-shape">💕</div>
					<div class="couple-emoji">👫</div>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* CSS Custom Properties for Better Responsiveness */
	:root {
		--container-padding: clamp(1rem, 5vw, 2rem);
		--section-padding: clamp(2rem, 8vw, 4rem);
		--hero-title-size: clamp(2.5rem, 8vw, 4rem);
		--hero-subtitle-size: clamp(1rem, 3vw, 1.2rem);
		--section-title-size: clamp(2rem, 6vw, 3rem);
		--card-title-size: clamp(1.5rem, 4vw, 2rem);
		--text-size: clamp(0.875rem, 2.5vw, 1rem);
		--about-title-size: clamp(1.8rem, 5vw, 2.5rem);
	}

	/* Add global box-sizing and overflow prevention */
	* {
		box-sizing: border-box;
	}

	html, body {
		overflow-x: hidden;
		max-width: 100vw;
	}

	/* Hero Section */
	.hero {
		background: linear-gradient(135deg, var(--cream) 0%, var(--beige-light) 50%, var(--beige-dark) 100%);
		padding: var(--section-padding) 0;
		min-height: clamp(60vh, 80vh, 90vh);
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
		width: 100%;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="10" cy="10" r="1" fill="rgba(60,36,21,0.1)"/><circle cx="90" cy="30" r="1.5" fill="rgba(60,36,21,0.1)"/><circle cx="30" cy="90" r="1" fill="rgba(60,36,21,0.1)"/></svg>');
		animation: float 25s ease-in-out infinite;
	}

	.hero-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--container-padding);
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: clamp(2rem, 5vw, 3rem);
		align-items: center;
		position: relative;
		z-index: 1;
		width: 100%;
		box-sizing: border-box;
	}

	.hero-title {
		font-family: 'Dancing Script', cursive;
		font-size: var(--hero-title-size);
		font-weight: 700;
		color: var(--chocolate-dark);
		display: block;
		line-height: 1;
		text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
		margin-bottom: 0.5rem;
		word-wrap: break-word;
		overflow-wrap: break-word;
		hyphens: auto;
	}

	.hero-subtitle {
		font-size: var(--hero-subtitle-size);
		color: var(--chocolate-medium);
		font-weight: 500;
		display: block;
		margin-bottom: 1.5rem;
		opacity: 0.9;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.hero-description {
		font-size: var(--text-size);
		color: var(--text-light);
		margin-bottom: 2rem;
		line-height: 1.7;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.hero-description strong {
		color: var(--chocolate-dark);
		font-weight: 600;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		width: 100%;
	}

	.hero-image {
		position: relative;
		height: clamp(250px, 50vw, 400px);
		display: flex;
		align-items: center;
		justify-content: center;
		width: 100%;
	}

	.bombom-img {
		width: clamp(200px, 40vw, 400px);
		height: auto;
		max-width: 100%;
		object-fit: contain;
	}

	.floating-emoji {
		position: relative;
		animation: bounce 2s ease-in-out infinite;
		filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.2));
		max-width: 100%;
		height: auto;
	}

	/* Featured Products Section */
	.featured-products {
		padding: var(--section-padding) 0;
		background: var(--warm-white);
		width: 100%;
		overflow: hidden;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--container-padding);
		width: 100%;
		box-sizing: border-box;
	}

	.section-title {
		font-family: 'Dancing Script', cursive;
		font-size: var(--section-title-size);
		color: var(--chocolate-dark);
		text-align: center;
		margin-bottom: 3rem;
		position: relative;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.section-title::after {
		content: '';
		position: absolute;
		bottom: -15px;
		left: 50%;
		transform: translateX(-50%);
		width: clamp(60px, 15vw, 80px);
		height: 4px;
		background: var(--gold);
		border-radius: 2px;
	}

	.products-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(min(300px, 100%), 1fr));
		gap: clamp(1.5rem, 4vw, 2rem);
		width: 100%;
	}

	.product-card {
		background: linear-gradient(135deg, var(--cream), var(--beige-light));
		padding: clamp(1.5rem, 4vw, 2.5rem) clamp(1rem, 3vw, 2rem);
		border-radius: 20px;
		text-align: center;
		box-shadow: 0 8px 25px rgba(60, 36, 21, 0.1);
		transition: all 0.3s ease;
		border: 2px solid transparent;
		position: relative;
		overflow: hidden;
		width: 100%;
		box-sizing: border-box;
	}

	.product-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, var(--chocolate-light), var(--chocolate-medium));
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.product-card:hover {
		box-shadow: 0 15px 35px rgba(60, 36, 21, 0.2);
		border-color: var(--chocolate-light);
	}

	.product-card:hover::before {
		transform: scaleX(1);
	}

	.product-card.featured {
		border-color: var(--gold);
		background: linear-gradient(135deg, #fff9e6, var(--beige-light));
	}

	.product-card.featured::before {
		background: linear-gradient(90deg, var(--gold), #f4d03f);
		transform: scaleX(1);
	}

	.product-badge {
		position: absolute;
		top: 15px;
		right: 15px;
		background: var(--gold);
		color: var(--chocolate-dark);
		padding: 0.4rem 0.8rem;
		border-radius: 15px;
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
	}

	.product-icon {
		width: clamp(2.5rem, 8vw, 3.5rem);
		height: clamp(2.5rem, 8vw, 3.5rem);
		margin: 0 auto 1.2rem;
		display: block;
		animation: bounce 2s ease-in-out infinite;
		object-fit: contain;
	}

	.product-card h3 {
		font-family: 'Dancing Script', cursive;
		font-size: var(--card-title-size);
		color: var(--chocolate-dark);
		margin-bottom: 1rem;
		line-height: 1.2;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.product-card p {
		color: var(--text-light);
		line-height: 1.6;
		font-size: var(--text-size);
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	/* About Preview Section */
	.about-preview {
		padding: var(--section-padding) 0;
		background: linear-gradient(135deg, var(--beige-light) 0%, var(--cream) 100%);
		width: 100%;
		overflow: hidden;
	}

	.about-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: clamp(2rem, 5vw, 3rem);
		align-items: center;
		width: 100%;
	}

	.about-text h2 {
		font-family: 'Dancing Script', cursive;
		font-size: var(--about-title-size);
		color: var(--chocolate-dark);
		margin-bottom: 1.5rem;
		position: relative;
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.about-text h2::after {
		content: '';
		position: absolute;
		bottom: -10px;
		left: 0;
		width: clamp(50px, 12vw, 60px);
		height: 3px;
		background: var(--gold);
		border-radius: 2px;
	}

	.about-text p {
		color: var(--text-light);
		line-height: 1.7;
		margin-bottom: 1.2rem;
		font-size: var(--text-size);
		word-wrap: break-word;
		overflow-wrap: break-word;
	}

	.about-text strong {
		color: var(--chocolate-dark);
		font-weight: 600;
	}

	.about-visual {
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		height: clamp(200px, 30vw, 250px);
		width: 100%;
	}

	.heart-shape {
		position: absolute;
		font-size: clamp(4rem, 12vw, 6rem);
		color: var(--gold);
		opacity: 0.3;
		animation: pulse 3s ease-in-out infinite;
	}

	.couple-emoji {
		font-size: clamp(2.5rem, 8vw, 3.5rem);
		position: relative;
		z-index: 2;
		animation: bounce 2s ease-in-out infinite;
	}

	/* Button Styles */
	.btn {
		padding: clamp(0.8rem, 2.5vw, 1rem) clamp(1.5rem, 4vw, 2rem);
		border-radius: 50px;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		border: 2px solid transparent;
		font-size: clamp(0.875rem, 2.5vw, 0.95rem);
		box-sizing: border-box;
		min-height: 44px; /* Touch target size */
	}

	.btn-primary {
		background: linear-gradient(45deg, var(--chocolate-medium), var(--chocolate-dark));
		color: var(--warm-white);
		box-shadow: 0 4px 15px rgba(60, 36, 21, 0.3);
	}

	.btn-primary:hover {
		box-shadow: 0 6px 20px rgba(60, 36, 21, 0.4);
		background: linear-gradient(45deg, var(--chocolate-dark), var(--chocolate-medium));
	}

	.btn-secondary {
		background: transparent;
		color: var(--chocolate-dark);
		border-color: var(--chocolate-medium);
	}

	.btn-secondary:hover {
		background: var(--chocolate-medium);
		color: var(--warm-white);
	}

	.btn-outline {
		background: transparent;
		color: var(--chocolate-dark);
		border-color: var(--chocolate-light);
	}

	.btn-outline:hover {
		background: var(--chocolate-light);
		color: var(--warm-white);
	}

	.btn-icon {
		font-size: 1rem;
	}

	/* Animations */
	@keyframes float {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-15px); }
	}

	@keyframes bounce {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-8px); }
	}

	@keyframes pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.05); }
	}

	/* Responsive Design with Enhanced Breakpoints */
	
	/* Large Tablets and Small Desktops */
	@media (max-width: 1024px) {
		.hero-content {
			gap: 2rem;
		}
		
		.products-grid {
			grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		}
	}

	/* Tablets */
	@media (max-width: 768px) {
		.hero-content {
			grid-template-columns: 1fr;
			text-align: center;
		}

		.hero-image {
			order: -1;
		}

		.about-content {
			grid-template-columns: 1fr;
			text-align: center;
		}

		.about-text h2::after {
			left: 50%;
			transform: translateX(-50%);
		}

		.hero-buttons {
			justify-content: center;
		}

		.products-grid {
			grid-template-columns: 1fr;
		}
	}

	/* Small Tablets and Large Phones */
	@media (max-width: 640px) {
		.hero-buttons {
			flex-direction: column;
			align-items: center;
		}

		.btn {
			width: 100%;
			max-width: 280px;
			justify-content: center;
		}
	}

	/* Mobile Phones */
	@media (max-width: 480px) {
		:root {
			--container-padding: 1rem;
			--section-padding: 2rem;
		}

		.hero {
			min-height: 60vh;
		}

		.section-title {
			margin-bottom: 2rem;
		}

		.product-card {
			padding: 1.5rem 1rem;
		}
	}

	/* Small Mobile Phones */
	@media (max-width: 360px) {
		:root {
			--container-padding: 0.75rem;
			--section-padding: 1.5rem;
		}

		.hero {
			min-height: 50vh;
		}

		.product-card {
			padding: 1.25rem 0.75rem;
		}

		.btn {
			padding: 0.75rem 1.25rem;
			font-size: 0.875rem;
		}
	}

	/* Very Small Screens */
	@media (max-width: 320px) {
		:root {
			--container-padding: 0.5rem;
		}

		.hero-buttons {
			gap: 0.75rem;
		}

		.btn {
			padding: 0.7rem 1rem;
			min-height: 40px;
		}
	}

	/* High DPI Displays */
	@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
		.floating-emoji,
		.product-icon {
			image-rendering: -webkit-optimize-contrast;
			image-rendering: crisp-edges;
		}
	}

	/* Landscape orientation for mobile */
	@media (max-height: 500px) and (orientation: landscape) {
		.hero {
			min-height: 80vh;
			padding: 1rem 0;
		}

		.hero-content {
			gap: 1.5rem;
		}

		.hero-image {
			height: 200px;
		}
	}

	/* Reduced motion preferences */
	@media (prefers-reduced-motion: reduce) {
		.floating-emoji,
		.heart-shape,
		.couple-emoji {
			animation: none;
		}
		
		.hero::before {
			animation: none;
		}
	}

	/* Print styles */
	@media print {
		.hero {
			min-height: auto;
			padding: 2rem 0;
		}
		
		.btn {
			border: 1px solid #000;
			color: #000 !important;
			background: transparent !important;
		}
		
		.floating-emoji,
		.product-icon {
			filter: none;
		}
	}
</style>
